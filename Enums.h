
// 定义枚举常量

// 定义游戏块类型
enum BlockType
{
	SQUARE_BLOCK,		// 正方形
	T_BLOCK,			// T形
	L_BLOCK,			// L形
	BACKWARDS_T_BLOCK,	// 反向L形
	STRAIGHT_BLOCK,		// 长方形
	S_BLOCK,			// S形
	BACKWARDS_S_BLOCK,	// 反向S形
};

// 定义方向枚举
enum Direction
{
	LEFT,
	RIGHT,
	DOWN,
	NONE
};